{
  "name": "cors-test",
  "version": "0.0.1",
  "private": true,
  "description": "CORS header tester — Cloudflare Worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.114.0"
  }
}
